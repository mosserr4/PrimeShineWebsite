{
  "name": "prime-shine-website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy --output=_site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0"
  }
}
